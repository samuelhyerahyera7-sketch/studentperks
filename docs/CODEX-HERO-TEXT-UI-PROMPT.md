# Codex task: implement approved StudentPerks hero text UI

Update only the StudentPerks homepage hero text/UI treatment using `docs/design-references/HERO-TEXT-UI.md` as the source of truth.

Use `assets/desktop/decor/hero-green-underline.svg` for the lime hand-drawn underline. Do not use CSS text-decoration as a substitute. Use `assets/desktop/hero-slogan.png` as the reference/artwork for the handwritten `Same students / brighter tomorrows` accent if the correct font is not already available.

Keep the following copy exact:

Same students
brighter tomorrows

Student life costs less here.

Discover verified deals, events and opportunities made for South African students.

Join StudentPerks →

Sign in

Keep all wording live and accessible. Preserve the Table Mountain hero image, current routes, authentication and responsive mobile layout. Match the approved desktop hero reference and test at 1024, 1280, 1440, 1920, 360, 390 and 430 px. Run the build before completion.
